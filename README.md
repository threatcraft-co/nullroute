# nullroute
Nullroute is a headless cross-platform daemon that silently strips tracking parameters from URLs the moment you copy them. No UI. No network calls. Runs at startup, stays out of your way. Built on ClearURLs rules. Your clipboard, cleaned before you ever hit paste.
